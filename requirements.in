# FastAPI and ASGI server
fastapi>=0.111.0,<1.0.0
# Base uvicorn for all platforms
uvicorn
# High-performance extras for Linux/Production only
uvloop; sys_platform != 'win32'
httptools; sys_platform != 'win32'
# Standard extras for Windows/Local development only
watchfiles; sys_platform == 'win32'
colorama; sys_platform == 'win32'
gunicorn

# Pydantic and Validation
pydantic[email]>=2.7.0,<3.0.0
pydantic-settings
openpyxl
slack-sdk

# Database & ORM
prisma>=0.13.0,<1.0.0
asyncpg
redis
apscheduler[redis]
# HTTP Client
httpx==0.28.0
aiohttp

# Security
PyJWT[crypto]
passlib[bcrypt]
bcrypt>=4.0.0
python-multipart

# Utilities
python-dotenv
python-dateutil
aiosmtplib

# Testing
pytest
pytest-asyncio
pytest-mock
pytest-cov

# Observability
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
opentelemetry-instrumentation-fastapi