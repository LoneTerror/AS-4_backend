apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: recognition-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /v1/recognition/$2
spec:
  ingressClassName: nginx
  rules:
  - host: api.rnr.local
    http:
      paths:
      - path: /v1/recognitions(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: recognition-service
            port:
              number: 8005