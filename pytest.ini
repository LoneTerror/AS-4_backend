[pytest]
asyncio_mode = auto
# Tell pytest to look for tests starting from the src directory
testpaths = src

# Ensure the src directory is added to PYTHONPATH for clean imports
pythonpath = src

# Pattern for finding test files
python_files = test_*.py

# Optional: Add clear console output
addopts = -v --tb=short